# Superseded

This unfinished change was superseded on 2026-04-24 by the layered code-understanding direction:

- `openspec/changes/supi-tree-sitter/` for the reusable structural Tree-sitter substrate
- `openspec/changes/extract-project-root-utils/` for shared project/root detection utilities
- `openspec/changes/supi-code-intelligence/` for the single agent-facing code intelligence extension

The old active change directory was moved here to keep historical context while removing it from the active OpenSpec change list.
