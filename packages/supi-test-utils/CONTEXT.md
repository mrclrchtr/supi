# supi-test-utils

Shared test utilities for SuPi packages: PI mock factories, context builders, and test helpers. Not published as an installable extension.

See also: root `CONTEXT.md` (Infrastructure Package, DevTool).
