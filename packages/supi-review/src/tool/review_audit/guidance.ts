export const toolDescription =
  "List local reviewer replays or inspect one through Replay Outline, selected-message, or raw views. Start with the metadata-only Replay Outline; message and raw views can contain repository evidence and tool output. Available only when review auditing is enabled.";

export const promptSnippet = "List or navigate local reviewer replays";

export const promptGuidelines: string[] = [];
