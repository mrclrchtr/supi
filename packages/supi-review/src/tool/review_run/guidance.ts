export const toolDescription =
  "Run independent review tasks concurrently under an Inspection-only protocol against one frozen Review Target. Use for code reviews or criteria-based inspections instead of generic subagents, not for exploration or simple direct checks.";

export const promptSnippet = "Run independent inspection-only reviews";

export const promptGuidelines: string[] = [];
