/** Selection guidance for the agent_run tool. */
export const toolDescription =
  "Delegate repository evidence gathering or focused coding tasks to Agent Profiles. Read-only profiles can run concurrently; mutation-capable profiles require one task. Foreground only; no recursive delegation.";
