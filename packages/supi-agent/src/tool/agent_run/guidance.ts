/** Selection guidance for the agent_run tool. */
export const toolDescription =
  "Delegate repository evidence gathering or focused coding tasks to Agent Profiles. Use review_run for code reviews or criteria-based inspections. Only batches in which every selected profile is read-only can contain multiple tasks; any mutation-capable batch must contain one task. Foreground only; no recursive delegation.";
