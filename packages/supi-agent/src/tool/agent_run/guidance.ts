/** Selection guidance for the agent_run tool. */
export const toolDescription =
  "Start foreground sub-agents with selected Agent Profiles. Only batches where every selected profile is read-only can contain multiple tasks; mutation-capable batches must contain one task.";
