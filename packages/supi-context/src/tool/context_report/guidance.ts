// Tool description and prompt snippet for the context_report agent tool.

export const toolDescription =
  "Report current context capacity. Omit mode for a concise, constant-shape pressure snapshot; use mode: full only when diagnostic attribution is needed.";

export const promptSnippet = "report context pressure (full mode adds diagnostics)";
