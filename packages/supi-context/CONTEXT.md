# supi-context

Detailed context usage report via `/supi-context` command. Shows token breakdown, context window, compaction, injected files, and guideline sources.

See also: root `CONTEXT.md` (Context Usage Monitor, Context Efficiency).
