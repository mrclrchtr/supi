# Task 6: Throw policy + CLAUDE.md gotcha rewrite (H)

Throw from execute() for whole-tool capability-unavailable in code_graph, code_resolve, code_refactor_plan (match code_find which already throws) — route.preferred==="unavailable" / no provider at all → throw (isError:true). KEEP error text for self-correctable invalid usage (missing target/params — the ~6 tests asserting `result.content` contains "requires a target"/"requires file, line, and character"/"requires a non-empty query"/"planId is required" stay valid). KEEP best-effort per-relation/per-target unavailable as notes (code_graph relations, code_context sections, code_resolve candidates). Then rewrite the package CLAUDE.md "throw" gotcha to state the 3-way policy: throw=whole-tool-unavailable; error-text=self-correctable invalid usage; notes=best-effort partial. Run `pnpm verify:ai`; audit tests covering whole-tool-unavailable paths.
