# Task 1: Executor contract + adapter spine (A-contract + B)

Change `CodeIntelligenceToolDefinitionSpec.run` ctx type to `{ cwd: string; signal?: AbortSignal; onUpdate?: AgentToolUpdateCallback }` and update the `run` type in tool-specs.ts. Rewrite the `register-tools.ts` adapter to pass `{ cwd: ctx.cwd, signal, onUpdate }` and to run the returned `content` through `truncateHead` (import `truncateHead`, `DEFAULT_MAX_LINES`, `DEFAULT_MAX_BYTES` from `@earendil-works/pi-coding-agent`), appending a `[truncated: N of M lines (X of Y)]` notice when truncated. Add optional per-spec `maxLines`/`maxBytes` + `spillToTempFile` fields (unused/`false` for now except heavy tools). `details` never truncated. signal/onUpdate optional so existing direct executor calls still compile. Run `pnpm verify:ai`.
