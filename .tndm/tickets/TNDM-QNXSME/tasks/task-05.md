# Task 5: Prompt-surface dedupe + truncation clauses (D)

Rewrite tool-specs.ts descriptions/guidelines per ADR 0005: descriptions own the complete always-on contract + full mechanics (mode/kind matrix, anchored-snapping, targetId-precedence, side effects, ordering) PLUS a one-line truncation clause ("Output is truncated to 2000 lines / 50KB."); guidelines shrink to ≤5 bullets each naming the tool — sibling-selection steering + the 1-2 execution rules that most prevent misuse, no mechanics re-list. PRESERVE test-pinned substrings in extension-registration.test.ts: code_find description (`requires \`kind\``, the six AST kinds definition/import/export/call/type/interface, `does not silently fall back`, `not by symbol identity`), code_find guidelines (`code_find with \`mode: "ast"\` requires \`kind\``, `code_find with \`mode: "semantic"\` does not fall back`, `code_find with \`mode: "text"\`, \`mode: "regex"\`, or \`mode: "semantic"\` does not accept \`kind\``, `symbol-identity-aware callers`), code_health (snippet/guidelines contain `coverage` and `unused`). Run `pnpm verify:ai`.
