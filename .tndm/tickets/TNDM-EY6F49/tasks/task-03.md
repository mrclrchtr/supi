# Task 3: Add explicit cross-family orchestration guidance to supi-code-intelligence without adding a new runtime layer

Keep `packages/supi-code-intelligence/src/tool/tool-specs.ts` as the source of truth for `code_*` tools, and update `packages/supi-code-intelligence/src/tool/guidance.ts` to add explicit cross-family orchestration guidance: when to use `code_brief` / `code_map`, when to prefer semantic `lsp_*` tools, when to prefer structural `tree_sitter_*` tools, and when `code_pattern` is the intentional heuristic/search escape hatch. Keep `packages/supi-code-intelligence/src/tool/register-tools.ts` thin, and keep `packages/supi-code-intelligence/src/code-intelligence.ts` limited to overview injection plus tool registration—do not add any session-scoped cache or service. Update `packages/supi-code-intelligence/README.md` so it clearly states that installing this package exposes all three tool families. Cover the behavior with `packages/supi-code-intelligence/__tests__/unit/guidance.test.ts`, `packages/supi-code-intelligence/__tests__/unit/extension-registration.test.ts`, and `packages/supi-code-intelligence/__tests__/unit/tool-adapters.test.ts`.
