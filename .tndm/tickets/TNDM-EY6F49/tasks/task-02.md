# Task 2: Align supi-lsp docs and regression coverage with the split lsp_* expert tool surface

Keep `packages/supi-lsp/src/tool/tool-specs.ts` as the package-local source of truth for the split `lsp_*` tools, and make any needed cleanup in `packages/supi-lsp/src/tool/guidance.ts`, `packages/supi-lsp/src/tool/register-tools.ts`, and `packages/supi-lsp/src/handlers/session-lifecycle.ts` so registration stays thin and spec-driven. Update `packages/supi-lsp/README.md` to replace the stale `lsp_lookup` / `lsp_refactor` documentation with the actual split `lsp_hover`, `lsp_definition`, `lsp_references`, `lsp_implementation`, `lsp_document_symbols`, `lsp_workspace_symbols`, `lsp_diagnostics`, `lsp_rename`, `lsp_code_actions`, and `lsp_recover` tool surface. Backstop the public surface with `packages/supi-lsp/__tests__/unit/tool-specs.test.ts`, `packages/supi-lsp/__tests__/unit/guidance.test.ts`, and `packages/supi-lsp/__tests__/unit/focused-tools.test.ts`.
