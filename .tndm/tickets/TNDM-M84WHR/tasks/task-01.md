# Task 1: Delete legacy execute-brief.ts and execute-affected.ts, migrate tests

Delete `src/tool/execute-brief.ts` and `src/tool/execute-affected.ts` — both are dead production code (no source file imports them). Migrate 5 test files and the test helper to use new V2 executors directly. Update CLAUDE.md directory tree.
