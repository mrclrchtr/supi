schema_version = 1
id = "TNDM-DMD573"
title = "supi-security: package change detection and audit for installed npm extensions"

type = "feature"
priority = "p2"

depends_on = []
tags = ["audit", "security"]
